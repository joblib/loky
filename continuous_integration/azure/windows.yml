
parameters:
  name: ''
  vmImage: ''
  matrix: []

jobs:
- job: ${{ parameters.name }}
  pool:
    vmImage: ${{ parameters.vmImage }}
  variables:
    VIRTUALENV: 'testvenv'
    JUNITXML: 'test-data.xml'
    LOKY_MAX_DEPTH: 5
    COVERAGE_PROCESS_START: '.coveragerc'
  strategy:
    matrix:
      ${{ insert }}: ${{ parameters.matrix }}

  steps:
    - task: UsePythonVersion@0
      displayName: Get Python for Python tools.
      inputs:
        versionSpec: '3.7'
        addToPath: false
      name: pyTools

    - script: $(pyTools.pythonLocation)/bin/pip install --upgrade tox
      displayName: Install Python-based tools.

    - task: UsePythonVersion@0
      inputs:
        versionSpec: '$(python.version)'
        architecture: 'x64'
      displayName: Use cached Python $(python.version) for tests.

    - bash: $(pyTools.pythonLocation)/bin/tox -e $(tox.env)
      displayName: 'run tox -e $(tox.env)'

    - task: PublishTestResults@2
      inputs:
        testResultsFiles: '$(JUNITXML)'
        testRunTitle: ${{ format('{0}-$(Agent.JobName)', parameters.name) }}
      displayName: 'Publish Test Results'
      condition: succeededOrFailed()
    - script: |
        continuous_integration\\azure\\upload_codecov.cmd
      displayName: 'Upload to codecov'
      condition: succeeded()
